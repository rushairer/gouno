version: '2'
linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - bodyclose
    - errorlint
formatters:
  enable:
    - gofmt
    - goimports
